import { Context, TableMeta } from '../types';
interface ImageItemMeta {
    height: number;
    width: number;
}
interface FileItem {
    id: string;
    link: string;
    image_meta?: ImageItemMeta;
}
export declare class FilesEndpoint {
    #private;
    constructor(context: Context, tableMeta: TableMeta);
    upload(file: Uint8Array | Blob): Promise<FileItem>;
}
export {};
